type Props = {
	task_content: string
	is_complete: boolean
	date_created: Date
}

const SingleTaskDisplay = ({
	task_content,
	is_complete,
	date_created
}: Props) => {
	return <div>SingleTaskDisplay</div>
}

export default SingleTaskDisplay
